# Common Module

The `common` module contains common data structures and utilities that are used throughout the kernel.

This module contains the following files:

*   `aligned_box.rs`: This file contains the `AlignedBox` struct, which is used to allocate memory with a specific alignment.
*   `int_like.rs`: This file contains the `IntLike` trait, which is used to define integer-like types.
