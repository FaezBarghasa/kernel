# DTB Module

The `dtb` module is responsible for parsing the Device Tree Blob (DTB) that is provided by the bootloader. The DTB contains information about the hardware devices that are present on the system.

This module contains the following files:

*   `irqchip.rs`: This file contains the code for parsing the interrupt controller information from the DTB.
