# Asm Module

The `asm` module contains the assembly code for the kernel. This module is responsible for handling the low-level details of the supported architectures.

This module contains the following submodules:

*   `x86`: This submodule contains the assembly code for the x86 architecture.
*   `x86_64`: This submodule contains the assembly code for the x86-64 architecture.
